# Processed Data

Row-level processed CSV files are not published in this repository.

The local project contains video-level and sample-index files used to generate the aggregate tables and figures in `results/`, but those files include platform video IDs, author names, titles, or local file paths. They should remain in local or restricted storage together with raw videos, comments, extracted frames, and comment-level prediction files.
